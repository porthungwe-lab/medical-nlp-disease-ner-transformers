# This folder holds training outputs.
# Model weights (results/model/) and logs (results/logs/) are git-ignored because they are large.
# The committed result files (test_metrics.json, test_metrics.csv, run_summary.txt) are created when you run src/train_ner.py.
